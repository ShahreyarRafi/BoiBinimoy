"use client"

const page = () => {
    return (
        <div>
            <h1 className="flex justify-center items-center min-h-[100vh]">All the content place here.</h1>
        </div>
    );
};

export default page;