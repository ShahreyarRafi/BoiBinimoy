"use client"

import Writer from '@/components/UserDashboard/Writer/Writer';

const page = () => {
    return (
        <div>
            <Writer></Writer>
        </div>
    );
};

export default page;